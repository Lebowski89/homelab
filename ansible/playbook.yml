---

################################
# BOOTSTRAP
################################

- name: Deploy NetBox
  hosts: localhost
  connection: local
  become: true
  gather_facts: false

  vars:
    ansible_python_interpreter: /opt/ansible/ansible-venv/bin/python

    timezone: Australia/Melbourne
    docker_host_puid: "1000"
    docker_host_pgid: "1000"
    docker_host_appdata_root: /opt

    # Docker Swarm node name, not Ansible inventory name.
    netbox_swarm_node_hostname: mgt

  tasks:
    - name: Include NetBox bootstrap role
      ansible.builtin.include_role:
        name: netbox
        apply:
          tags:
            - never
      tags:
        - never
        - netbox_bootstrap
        - netbox_deploy
        - netbox_recreate
        - netbox_remove

################################
# DEPLOY HOMELAB
################################

- name: Deploy homelab services
  hosts: tags_skynet
  become: true

  pre_tasks:
    - name: Gather packages
      when: inventory_hostname == docker_services_primary_manager
      ansible.builtin.package_facts:
        manager: auto
      tags: always

    - name: Resolve host timezone variable
      tags:
        - infisical_deploy
        - infisical_recreate
        - infisical_podman_deploy
        - infisical_podman_recreate
        - deploy
        - update
        - remove
        - recreate
        - bootstrap
        - drift
        - reconcile
      block:
        - name: Set timezone variable on manager
          when: inventory_hostname == docker_services_primary_manager
          ansible.builtin.set_fact:
            timezone: "{{ ansible_facts.timezone | default('Australia/Melbourne') }}"

        - name: Timezone output
          when: inventory_hostname == docker_services_primary_manager
          ansible.builtin.debug:
            msg: "{{ timezone }}"

        - name: Share timezone to all hosts
          when: inventory_hostname != docker_services_primary_manager
          ansible.builtin.set_fact:
            timezone: "{{ hostvars[docker_services_primary_manager].timezone }}"

  tasks:

    ################################
    # OS (CONFIG/SETTINGS)
    ################################

    - name: Include Ubuntu role
      when:
        - ansible_facts.os_family | default("") == "Debian"
        - ansible_facts.distribution | default("") == "Ubuntu"
      ansible.builtin.include_role:
        name: ubuntu
      tags:
        - ubuntu
        - ubuntu_apt
        - ubuntu_repo
        - ubuntu_venv
        - ubuntu_requirements
        - ubuntu_skynet
        - ubuntu_sysctl
        - ubuntu_pam
        - ubuntu_network
        - ubuntu_netplan

    ################################
    # DOCKER
    ################################

    - name: Include Docker role
      when: "'tags_docker' in group_names"
      ansible.builtin.include_role:
        name: docker
      tags:
        - docker
        - docker_install
        - docker_prune
        - docker_prune_dangling
        - docker_prune_unused
        - docker_prune_volumes
        - docker_prune_timer
        - docker_prune_unraid_user_script
        - docker_swarm
        - docker_swarm_init
        - docker_swarm_join
        - docker_swarm_network
        - docker_swarm_labels

    ################################
    # HUGO (INIT/THEME)
    ################################

    - name: Include Hugo role
      when: inventory_hostname == docker_services_primary_manager
      ansible.builtin.include_role:
        name: hugo
      tags:
        - hugo_bootstrap
        - hugo_submodule

    ################################
    # INFISICAL (STACK)
    ################################

    - name: Include Infisical role
      when: inventory_hostname == docker_services_primary_manager
      ansible.builtin.include_role:
        name: infisical
      tags:
        - infisical_bootstrap
        - infisical_deploy
        - infisical_recreate
        - infisical_remove

    ################################
    # TERRAFORM
    ################################

    - name: Include OpenTofu role
      when: "'tags_opentofu' in group_names"
      ansible.builtin.include_role:
        name: opentofu
      tags:
        - opentofu
        - opentofu_install
        - opentofu_pve_user

    ################################
    # POSTGRES (CLUSTER)
    ################################

    - name: Include Postgres role
      ansible.builtin.include_role:
        name: postgres
      tags:
        - postgres
        - postgres_apt
        - postgres_etcd
        - postgres_etcd_reset
        - postgres_patroni
        - postgres_patroni_reset
        - postgres_backup
        - postgres_restore
        - postgres_admin
        - postgres_admin_uptime_kuma
        - postgres_admin_nuke_node
        - postgres_admin_fix_owner
        - postgres_admin_update_pg_hba

    ################################
    # NODE EXPORTER (MONITORING)
    ################################

    - name: Include Node Exporter role
      ansible.builtin.include_role:
        name: node_exporter
        apply:
          tags: [node_exporter]
      vars:
        node_exporter_install_groups:
          - tags_ansible_manager
          - tags_docker
          - tags_postgres
      when:
        - node_exporter_install_groups | intersect(group_names) | length > 0
        - "'device_roles_storage' not in group_names"
      tags: [node_exporter]

    ################################
    # DOCKER_SERVICES
    ################################

    - name: Load all service definitions from group_vars/all/services/*.yml
      ansible.builtin.include_vars:
        dir: "{{ playbook_dir }}/group_vars/all/services"
        files_matching: ".*\\.ya?ml$"
        ignore_unknown_extensions: true
        extensions: [yml, yaml]
        name: svcfiles
      tags: [deploy, update, remove, recreate, bootstrap, drift]

    - name: Assert svcfiles loaded
      ansible.builtin.assert:
        that:
          - svcfiles is defined
          - (svcfiles | type_debug) == "dict"
          - (svcfiles | length) > 0
        fail_msg: "svcfiles is empty or not a dict. include_vars did not load your service files."
      tags: [deploy, update, remove, recreate, bootstrap, drift]

    - name: Use svcfiles as the services mapping
      ansible.builtin.set_fact:
        services: "{{ svcfiles }}"
      tags: [deploy, update, remove, recreate, bootstrap, drift]

    - name: Build docker service processing list from service definitions
      ansible.builtin.set_fact:
        docker_services_effective: "{{ _docker_services_effective_yaml | from_yaml }}"
      vars:
        _docker_services_effective_yaml: |
          {%- set out = [] -%}
          {%- for service_name, service_cfg in services.items() -%}
          {%- if service_cfg.enabled | default(true) | bool -%}
          {%- set service_tags = ([service_name] + (service_cfg.tags | default([]))) | unique | list -%}

          {%- if service_cfg.targets is defined and service_cfg.targets is mapping -%}
          {%- for target_name, target_cfg in service_cfg.targets.items() -%}
          {%- if target_cfg.enabled | default(true) | bool -%}
          {%- set target_tags = (service_tags + (target_cfg.tags | default([]))) | unique | list -%}
          {%- set _ = out.append({
                'name': service_name,
                'target': target_name,
                'tags': target_tags
              }) -%}
          {%- endif -%}
          {%- endfor -%}

          {%- else -%}
          {%- set _ = out.append({
                'name': service_name,
                'tags': service_tags
              }) -%}
          {%- endif -%}

          {%- endif -%}
          {%- endfor -%}
          {{ out | to_yaml }}
      tags: [deploy, update, remove, recreate, bootstrap, drift]

    - name: Assert docker service processing list was built
      ansible.builtin.assert:
        that:
          - docker_services_effective is defined
          - docker_services_effective is sequence
          - docker_services_effective | length > 0
        fail_msg: "docker_services_effective is empty. No enabled service definitions were discovered."
      tags: [deploy, update, remove, recreate, bootstrap, drift]

    - name: Drift | Initialise image drift summary
      when: inventory_hostname == docker_services_primary_manager
      ansible.builtin.set_fact:
        docker_services_image_drift: []
      run_once: true
      tags: [drift]

    - name: Drift | End non-primary hosts
      when:
        - "'drift' in (ansible_run_tags | default([]))"
        - inventory_hostname != docker_services_primary_manager
      ansible.builtin.meta: end_host
      tags: [drift]

    - name: Build selected docker service processing list
      ansible.builtin.set_fact:
        docker_services_selected: "{{ _docker_services_selected_yaml | from_yaml }}"
      vars:
        _docker_services_selected_yaml: |
          {%- set out = [] -%}
          {%- set run_all = docker_services_run_all | default(false) | bool -%}
          {%- set run_tags = ansible_run_tags | default([]) -%}

          {%- for item in docker_services_effective -%}
          {%- set item_tags = item.tags | default([]) -%}
          {%- if run_all
                or (run_tags | length == 0)
                or (item.name in run_tags)
                or ((item_tags | intersect(run_tags)) | length > 0) -%}
          {%- set _ = out.append(item) -%}
          {%- endif -%}
          {%- endfor -%}

          {{ out | to_yaml }}
      tags: [deploy, update, remove, recreate, bootstrap, drift]

    - name: Assert selected docker service processing list was built
      ansible.builtin.assert:
        that:
          - docker_services_selected is defined
          - docker_services_selected is sequence
          - docker_services_selected | length > 0
        fail_msg: >-
          No Docker service definitions matched the requested tag(s):
          {{ ansible_run_tags | default([]) | join(', ') }}.
      tags: [deploy, update, remove, recreate, bootstrap, drift]

    - name: Process each docker service
      ansible.builtin.include_role:
        name: docker_services
      vars:
        docker_services_service_cfg: "{{ services[item.name] | default({}) }}"
        docker_services_service_target: "{{ item.target | default(omit) }}"
        docker_services_role_prefix: "{{ item.name }}"
        docker_services_service_cfg_found: "{{ (services[item.name] is defined) | bool }}"
      loop: "{{ docker_services_selected }}"
      loop_control:
        label: "{{ item.name }}{{ ':' ~ item.target if item.target is defined else '' }}"
      when:
        - docker_services_service_cfg_found | bool
      tags: [deploy, update, remove, recreate, bootstrap, drift]

    - name: Drift | Report image drift summary
      when:
        - inventory_hostname == docker_services_primary_manager
        - hostvars[docker_services_primary_manager].docker_services_image_drift is defined
        - (hostvars[docker_services_primary_manager].docker_services_image_drift | length) > 0
      ansible.builtin.debug:
        msg: |
          Docker image drift detected:
          {% for drift_item in hostvars[docker_services_primary_manager].docker_services_image_drift | sort(attribute='label') %}
          - {{ drift_item.label }}
            stack:   {{ drift_item.stack }}
            service: {{ drift_item.service }}
            desired: {{ drift_item.desired }}
            current: {{ drift_item.current }}

          {% endfor %}
      run_once: true
      tags: [drift]

    - name: Drift | Import email notification tasks
      ansible.builtin.import_tasks: "{{ playbook_dir }}/roles/docker_services/tasks/sub_tasks/drift/notify_email.yml"
      tags: [drift]

    - name: Drift | Report no image drift found
      when:
        - inventory_hostname == docker_services_primary_manager
        - hostvars[docker_services_primary_manager].docker_services_image_drift is not defined
          or (hostvars[docker_services_primary_manager].docker_services_image_drift | length) == 0
      ansible.builtin.debug:
        msg: "No Docker service image drift detected."
      run_once: true
      tags: [drift]

    - name: Deploy all stacks
      when:
        - inventory_hostname == docker_services_primary_manager
        - not (ci_mode | default(false) | bool)
      ansible.builtin.include_tasks:
        file: "{{ playbook_dir }}/roles/docker_services/tasks/sub_tasks/deploy/all.yml"
        apply:
          tags: [deploy, update, recreate]
      run_once: true
      tags: [deploy, update, recreate]

    ################################
    # TESTING
    ################################

    - name: Test | Include Podman role
      when: inventory_hostname == docker_services_primary_manager
      ansible.builtin.include_role:
        name: podman
      tags:
        - podman
        - podman_install

    - name: Test | Include Infisical Podman role
      when: inventory_hostname == docker_services_primary_manager
      ansible.builtin.include_role:
        name: infisical_podman
      tags:
        - infisical_podman_bootstrap
        - infisical_podman_deploy
        - infisical_podman_recreate
        - infisical_podman_remove
