# Generated by Ansible. Do not edit manually.
http:
  routers:
    {{ podman_service.name }}-private:
      rule: Host(`{{ podman_service.name }}.{{ internal_zone }}`)
      entryPoints:
        - websecure-private
      middlewares:
        - default-private-chain
      service: {{ podman_service.name }}-host
      tls: {}
  services:
    {{ podman_service.name }}-host:
      loadBalancer:
        servers:
          - url: http://{{ podman_service.traefik.backend_host | default(hostvars[podman_service.traefik.backend_host_inventory].local_ip) }}:{{ podman_service.traefik.port }}
