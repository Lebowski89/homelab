[defaults]
# Where Ansible looks for collections
collections_path = collections

# Where Ansible looks for roles
roles_path = roles:test

# Other useful defaults
inventory = hosts.ini

log_path = /opt/ansible/ansible.log