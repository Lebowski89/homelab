---

puid: 1000
pgid: 1000

local_ip: 192.168.80.59

docker_manage_user: true
docker_user: "plex"
docker_swarm_hostname: "plex"

docker_host_puid: 1000
docker_host_pgid: 1000
docker_host_appdata_root: /opt

docker_swarm_node_labels:
  role: worker
  ansible_host: plex
  docker_services_host: docker_services_plex_host
  haproxy: "true"
  dns: technitium
