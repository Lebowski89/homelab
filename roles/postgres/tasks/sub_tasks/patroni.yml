---

- name: Assert Patroni node vars are defined
  ansible.builtin.assert:
    that:
      - postgres_patroni_node_name is defined
      - postgres_patroni_node_name | length > 0
      - local_ip is defined
      - local_ip | length > 0
      - postgres_patroni_superuser_pass is defined
      - postgres_patroni_superuser_pass | length > 0
      - postgres_patroni_replication_pass is defined
      - postgres_patroni_replication_pass | length > 0
    fail_msg: "Required Patroni variables are missing."

- name: Ensure Patroni config directory exists
  ansible.builtin.file:
    path: "{{ postgres_patroni_config_dir }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Ensure Patroni data directory exists
  ansible.builtin.file:
    path: "{{ postgres_patroni_data_dir }}"
    state: directory
    owner: postgres
    group: postgres
    mode: "0700"

- name: Stop and disable default PostgreSQL service
  ansible.builtin.systemd_service:
    name: postgresql
    state: stopped
    enabled: false

- name: Template Patroni config
  ansible.builtin.template:
    src: patroni.yml.j2
    dest: "{{ postgres_patroni_config_path }}"
    owner: root
    group: root
    mode: "0600"
  notify: Restart patroni
  no_log: true

- name: Enable and start Patroni
  ansible.builtin.systemd_service:
    name: patroni
    state: started
    enabled: true

- name: Wait for Patroni REST API
  ansible.builtin.wait_for:
    host: "{{ local_ip }}"
    port: "{{ postgres_patroni_restapi_port }}"
    timeout: 60
